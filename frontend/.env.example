# Copy to .env.production (or set in your host's build env) for deployment.
# Leave UNSET for local dev — Vite's proxy forwards /api to localhost:8000.
VITE_API_BASE_URL=https://your-backend.onrender.com
